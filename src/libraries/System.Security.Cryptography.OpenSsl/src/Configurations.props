<Project DefaultTargets="Build">
  <PropertyGroup>
    <PackageConfigurations>
      netcoreapp5.0-Unix;
      netcoreapp3.0;
      netstandard2.0;
    </PackageConfigurations>
    <BuildConfigurations>
      $(PackageConfigurations);
      $(NetCoreAppCurrent)-Unix;
      $(NetCoreAppCurrent);
    </BuildConfigurations>
  </PropertyGroup>
</Project>